# Разрешённые совпадения для tests/lint-no-secrets.sh.
#
# Формат: путь:совпадение — исключение действует только в этом файле.
# Новое совпадение не проходит молча: его нужно либо убрать из репозитория,
# либо осознанно добавить сюда. Смысл тот же, что у lint-run-guard.
#
# Прежде чем что-то сюда вписать, ответь себе: это выдуманный пример или
# кусок твоей настоящей инфраструктуры? Второе сюда не попадает никогда.

# Пример статической сети в справочнике манифеста
docs/MANIFEST.md:192.168.1.50
docs/MANIFEST.md:192.168.1.1

# Пример того, какой адрес НЕ надо оставлять в установщике Proxmox
docs/20-install-pve.md:mail@example.com

# Выдуманный ключ в тесте: проверяет, что ключи попадают в архив конфигурации
tests/run.sh:ssh-ed25519 AAAA
